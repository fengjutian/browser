import type { CSSProperties, HTMLAttributes, ReactNode } from 'react'
export function Row({ gutter = 0, className = '', style, children, ...props }: HTMLAttributes<HTMLDivElement> & { gutter?: number | [number, number]; children?: ReactNode }) { const [x, y] = Array.isArray(gutter) ? gutter : [gutter, 0]; return <div className={`ui-row ${className}`.trim()} style={{ marginInline: -x / 2, rowGap: y, ...style }} {...props}>{children}</div> }
export function Col({ span, xs, sm, lg, xl, className = '', style, children, ...props }: HTMLAttributes<HTMLDivElement> & { span?: number; xs?: number; sm?: number; lg?: number; xl?: number; children?: ReactNode }) { const base = span ?? xs ?? 24; const vars = { '--ui-col-base': base, '--ui-col-sm': sm ?? base, '--ui-col-lg': lg ?? sm ?? base, '--ui-col-xl': xl ?? lg ?? sm ?? base } as CSSProperties; return <div className={`ant-col ui-col ${className}`.trim()} style={{ ...vars, ...style }} {...props}>{children}</div> }
